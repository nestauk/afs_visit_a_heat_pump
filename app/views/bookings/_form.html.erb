<%= simple_form_for [host, event, booking] do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :email %>
  <%= f.input :quantity %>
  <%= f.input :notes %>

  <%= f.button :submit, 'Book', class: 'btn bg-blue-600 text-white' %>
<% end %>
